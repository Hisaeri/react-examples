import ReactDOM from 'react-dom';
import { Game } from './app';
import './index.css';
  
  // ========================================
  
  ReactDOM.render(
    <Game />,
    document.getElementById('root')
  );
  